export type TrendingVideos = {
  duration: number,
  thumbnail: string,
  title: string,
  uploadedDate: string,
  uploaderAvatar: string,
  uploaderUrl: string,
  uploaderVerified: boolean,
  url: string,
  views: number,
}[]
